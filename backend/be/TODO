1. db connection
    1.1. set default schema to api_user as budget (or set to api role or set on current connection idn)
    1.2. grant permissions to tables (idn why default schema permission to api_user don`t work)
    1.3. if default schema doesn`t work use budget.<table_name> as in get_shops()
    1.4. sync sql scripts with git repo
2. architecture
    2.1. global context
        2.1.1. db connection
        2.1.2. logger