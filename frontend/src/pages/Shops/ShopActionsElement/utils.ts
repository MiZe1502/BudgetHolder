import { Shop } from "../types";
import { MapItemData } from "../../../common/components/ActionElements/MapActionElement/utils";

export interface ShopActionsData {
    shopData: Shop;
    mapData: MapItemData[];
}