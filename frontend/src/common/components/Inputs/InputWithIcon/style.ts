import {css} from 'emotion';

import {FlexHorCenter} from "../../../styles/positioning";

export const style = {
    FlexHorCenter,

    Wrapper: css`
        position: relative;
    `,

    ButtonWrapper: css`
        position: absolute;
        right: 8px;
        padding-top: 14px;
    `
}