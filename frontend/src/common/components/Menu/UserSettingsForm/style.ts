import { css } from 'emotion'

import { SideMinorPadding, FlexVert } from "../../../../common/styles/positioning";

export const style = {
    SideMinorPadding,
    FlexVert,

    Form: css`
        padding-top: 8px;
        padding-bottom: 8px;
    `,
}