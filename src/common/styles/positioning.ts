import { css } from 'emotion'

export const FlexHor = css`
    display: flex;
    flex-direction: row;
`;

export const FlexHorCenter = css`
    display: flex;
    flex-direction: row;
    align-items: center;
`;

export const SideMainPadding = css`
    padding-right: 10%;
    padding-left: 10%;
`;

export const Overflowed = css`
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
`;
