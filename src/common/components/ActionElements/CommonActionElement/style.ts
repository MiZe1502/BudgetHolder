import { css } from 'emotion';

import { FlexHorCenter } from "../../../styles/positioning";

export const style = {
    FlexHorCenter,
    ActionElement: css`
        & > div {
            margin-right: 16px;
        }
      `
}